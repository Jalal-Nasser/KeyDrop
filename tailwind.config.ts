const config: Config = {
  // ... existing config
  theme: {
    extend: {
      colors: {
        // ... existing colors
        purple: {
          primary: '#805da8',
        },
      },
    },
  },
}