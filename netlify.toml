[build]
  command = "npm run build"
  publish = ".next"
  # Removed: functions = "netlify/functions" # This project uses Supabase Edge Functions, not Netlify Functions

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[plugins]]
  package = "@netlify/plugin-emails"
  inputs = {
    included_files = ["./emails/**"]
  }

# Headers for static assets (optional, but good practice)
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    "Cache-Control" = "public, max-age=31536000, immutable"

# Redirects (optional)
# [[redirects]]
#   from = "/old-path"
#   to = "/new-path"
#   status = 301